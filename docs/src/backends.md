# Backends

Here are the functions related to the backends. Users usually would just need to use the `set_GPUbackend()` or `set_CPUbackend()`.


```@docs
get_backend
```

```@docs
set_GPUbackend
```

```@docs
set_CPUbackend
```

```@docs
get_available_platforms
```

```@docs
get_available_GPUplatforms
```