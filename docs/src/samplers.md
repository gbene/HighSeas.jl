# Simulation samplers

Here are the objects that can be used to sample the simulation domain. It is possible to sample either a point (PointSampler), a section (SectionSampler) or contour data (ContourSampler)

```@docs
PointSampler
```

```@docs
SectionSampler
```

```@docs
ContourSampler
```